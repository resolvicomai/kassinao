/** Tetos persistentes e de leitura para conteúdo controlado por usuários. */
export const MAX_NOTES_PER_RECORDING = 500;
export const MAX_PRESENCE_IDENTITIES = 1_000;
export const MAX_PRESENCE_IDENTITIES_PER_RESPONSE = 100;

/** Ata externa: teto de arquivo antes de JSON.parse e tetos de materialização. */
export const MAX_MINUTES_BYTES = 1024 * 1024;
export const MAX_MINUTES_ITEMS_PER_COLLECTION = 200;
export const MAX_MINUTES_PARTICIPANTS_PER_RESPONSE = 100;
export const MAX_MINUTES_POINTS_PER_PARTICIPANT = 100;
